@model CodigoLimpioEntityFrameWork.Models.ideaColor

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ideaColor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Color.nombreColor)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Color.nombreColor)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Idea.nombreIdea)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Idea.nombreIdea)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
