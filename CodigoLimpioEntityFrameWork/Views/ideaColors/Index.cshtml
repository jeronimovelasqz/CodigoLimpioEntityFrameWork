@model IEnumerable<CodigoLimpioEntityFrameWork.Models.ideaColor>

@{
    ViewBag.Title = "GESTIÓN DE COLORES";
}

<h2>---</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Color.nombreColor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Idea.nombreIdea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Color.nombreColor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Idea.nombreIdea)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
